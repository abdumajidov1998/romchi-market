# Per-module ignores live in frontend/.gitignore and backend/.gitignore.
# Anything here applies to the repo root.

.DS_Store

# Editor / IDE
.claude/
.idea/workspace.xml

# Local H2 dev database (when running backend from repo root, ./data ends up here)
/data/
