services:
  - type: web
    name: romchi-market
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: "3000"
      - key: JWT_SECRET
        generateValue: true
